export class Course {
  constructor( public category: string, public name: string, public  instructors: string[], public starRating: number, public reviewCount: number, public originalCost: number, public currentCost: number, public description: number){};
}
