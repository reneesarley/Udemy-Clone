export class Course {
  constructor( category: string, name: string, instructors: string[], starRating: number, reviewCount: number, originalCost: number, currentCost: number, description: number){};
}
